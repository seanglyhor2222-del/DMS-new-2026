Uploaded font files (.ttf/.otf/.woff/.woff2) are stored here automatically
when an admin adds one from the "ពុម្ពអក្សរ" (Fonts) admin page.
Do not delete this file — it just keeps the folder present in git/zip exports.
